
backend/__pycache__/

# local secrets
backend/.env

# ML artifacts (large / regenerable)
# forecast_model.pkl is committed so Render can serve it without a retrain step
backend/ml_training_data.json
backend/training_report.json
backend/training_report.html
